<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Live meet-ups share location only while the meet-up is running, in a visible
         foreground service. We deliberately do NOT request ACCESS_BACKGROUND_LOCATION. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Sparkle photo moments — requested contextually when the user opens the camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Partner distance badge — requested contextually inside the Sparkle screen -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".RemindMe"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RemindMe"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <receiver
            android:name=".alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".alarm.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.AlarmForegroundService"
            android:foregroundServiceType="shortService" />

        <activity
            android:name=".ui.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.RemindMe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Invite deep link: https://gigi.iamanraj.com/join?code=XXXXXXXX opens the
                 app with the code pre-filled (the web page redirects to the Play Store
                 when the app isn't installed). android:autoVerify needs the matching
                 /.well-known/assetlinks.json served from the domain. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                    android:host="gigi.iamanraj.com"
                    android:pathPrefix="/join" />
            </intent-filter>
            <!-- Fallback custom scheme, always works without domain verification. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gigi" android:host="join" />
            </intent-filter>
            <!-- Where Spotify returns the user after the PKCE consent screen. Must match
                 the redirect URI registered in the Spotify developer dashboard exactly. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gigi" android:host="spotify-callback" />
            </intent-filter>
        </activity>

        <!-- Streams your position to an accepted meet-up. Runs only while the meet-up
             is live, with an ongoing notification, and stops on Done / expiry. -->
        <service
            android:name=".service.LiveTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Owns the update download. A 50-100 MB transfer has to be foreground work,
             or the OS kills the process partway through when the screen locks. -->
        <service
            android:name=".data.update.UpdateDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Floating chat-head bubble for incoming messages (WindowManager overlay). -->
        <service
            android:name=".service.ChatHeadService"
            android:exported="false" />

        <!-- Reads the phone's media session so "now playing" can be shared with your
             people (works with Spotify / YT Music / any player). The user grants this
             once via Settings › Notification access. -->
        <service
            android:name=".service.NowPlayingListenerService"
            android:exported="false"
            android:label="Gigi Now Playing"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.AlarmActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstancePerTask"
            android:taskAffinity=""
            android:theme="@style/Theme.RemindMe" />

        <service
            android:name=".service.ScribbleSyncService"
            android:exported="false" />

        <service
            android:name=".service.ScreensaverSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging|dataSync" />

        <activity
            android:name=".ui.screensaver.LockscreenScribbleActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Gigi.Overlay" />

        <activity
            android:name=".ui.screensaver.LockscreenPlayerActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:screenOrientation="sensor"
            android:configChanges="keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:theme="@style/Theme.Gigi.Overlay" />

        <activity
            android:name=".ui.sparkle.SparkleRevealActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Gigi.Overlay" />

        <!-- "Someone called a break" — full-screen over the lock screen, like a scribble. -->
        <activity
            android:name=".ui.breaks.BreakInviteActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Gigi.Overlay" />

        <activity
            android:name=".ui.LoveCardDeckActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Gigi.Overlay" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".service.BackgroundReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aman.gigi.action.KEEP_ALIVE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".service.MusicPlaybackService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
            <!-- Your person's Twigi on the home screen. Home-screen only: Android
                 phones have had no lockscreen widgets since API 21. -->
            <receiver
                android:name=".widget.TwigiStatusWidgetReceiver"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                </intent-filter>
                <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/twigi_status_widget_info" />
            </receiver>

            <receiver
            android:name=".widget.SharedCountdownWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/shared_countdown_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.LatestNoteWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/latest_note_widget_info" />
        </receiver>

        <activity
            android:name=".ui.chat.ChatBubbleActivity"
            android:exported="false"
            android:theme="@style/Theme.RemindMe.Bubble" />

    </application>

</manifest>
